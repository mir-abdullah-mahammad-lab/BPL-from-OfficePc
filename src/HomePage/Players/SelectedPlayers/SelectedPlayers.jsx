import React from 'react';

const SelectedPlayers = ({selectedPlayers}) => {
    console.log('playerARRAY', selectedPlayers)
    return (
        <div>
            Selected player
        </div>
    );
};

export default SelectedPlayers;